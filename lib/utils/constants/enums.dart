/// List of Enums
/// They connot be created inside a class.

enum TextSizes { small, medim, large }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods { paypal, googlePay, applePay, visa, masterCard }
